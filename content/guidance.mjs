// Учебные объяснения используются страницей и презентацией совместно.
export const guidance={
documents:[
{why:'Начинаем с источника, а не с готового кода. КИМ описывает действия людей; приложения дают изображения, шрифты и стиль. Из этого мы сами проектируем таблицы и формы.',actions:['Откройте «Материалы задания» на баннере. Прочитайте страницы 33–37 базового образца.','В README запишите роли, обязательные страницы и ограничения. Отдельно перечислите свои улучшения: они не становятся требованиями КИМ.'],check:'Можете показать источник каждого обязательного действия: регистрацию, вход, заявку, смену статуса, отзыв.'},
{why:'Существительные в условии помогают найти сущности, а глаголы — операции. «Пользователь создаёт заявку» означает две таблицы и связь, а не одну большую таблицу для всех полей.',actions:['Составьте таблицу: роль → действие → входные данные → результат.','Например: пользователь → выбирает помещение → номер зала и дата → новая заявка. Администратор → меняет статус → номер заявки → обновлённая запись.'],check:'Владелец заявки определяется после входа, а не вводится пользователем.'},
{why:'Готовых записей всех таблиц в КИМ нет. Изображения и шрифты берём из БУ; помещения в rooms.json — демонстрационные данные автора примера. Пользователи, заявки и отзывы появятся при работе приложения.',actions:['Создайте docs для исходного задания и схемы, static для ресурсов интерфейса.','Сохраните оригиналы отдельно. В SOURCES.md укажите источник каждого изображения и шрифта. Если выбираете бесплатный сток, проверьте условия использования конкретного файла.'],check:'Вы различаете исходные материалы, придуманный каталог и данные, которые вводит пользователь.'},
{why:'Первый коммит фиксирует начало работы, когда приложения ещё нет. Git хранит снимки выбранных файлов; git add подготавливает снимок, git commit записывает его в историю.',actions:['Откройте свою папку через «Файл → Открыть папку» в VS Code и создайте терминал.','Создайте README.md с названием и задачей, .gitignore с исключениями .venv/, __pycache__/, instance/, .env. Создайте папки docs и static.','Выполните команды ниже. Если Git просит имя и почту, задайте свои реальные значения через git config user.name и git config user.email.'],code:'git init\ngit add README.md .gitignore\ngit status\ngit commit -m "Начало задания 1: требования и структура"\ngit log --oneline',check:'В истории есть первый коммит; в нём только README и правила исключения, без паролей и рабочей базы.'},
{why:'Редактор изменяет файлы, сервер выполняет код, MySQL хранит данные, браузер показывает HTML. Это разные программы: открытие файла двойным щелчком не запускает сервер.',actions:['Для PHP запустите Apache и MySQL в XAMPP; проект находится в htdocs. Для Flask нужен Python, а из XAMPP — MySQL.','Проверьте git --version и доступность phpMyAdmin. Команды установки и запуска выбранного стека есть в «Примерах». Затем переходите к ER, не импортируя готовую схему вслепую.'],check:'Можете назвать программу, которая выполняет серверный код выбранного стека.'}
],
design:[
{why:'Стиль задаётся приложением БУ, а не оформлением учебного сайта. Меняем общие CSS-правила, чтобы все формы и страницы оставались одинаковыми.',actions:['Зафиксируйте начало задания 2: git commit --allow-empty -m "Начало задания 2". Затем откройте руководство в материалах. В CSS задайте переменные палитры и используйте их для фона, текста и кнопок.','Сравните каталог и регистрацию с образцом результата ниже. Красный цвет оболочки курса в приложение переносить не нужно.'],code:':root { --primary: #007bff; --ink: #0d47a1; --muted: #6c757d; --paper: #f8f9fa; }\nbody { background: var(--paper); color: var(--ink); }\nbutton { background: var(--primary); color: white; }',check:'Кнопки, заголовки и фон используют одну палитру.'},
{why:'Локальный шрифт работает без CDN. Путь в url отсчитывается от CSS-файла, а не от HTML.',actions:['Скопируйте PT Sans в static/fonts и сохраните лицензию.','В CSS добавьте @font-face с относительным путём к существующему файлу; назначьте font-family для body, input, select и button. Проверьте вкладку Network: нет 404.'],check:'При отключённом интернете буквы и размеры не меняются: 36/24/18/16 px по руководству.'},
{why:'Большое изображение замедляет страницу, а случайное растягивание искажает пропорции. Оригинал нужен для повторной обработки.',actions:['Выберите картинку из БУ, сохраните оригинал в docs и оптимизированную версию в static.','Задайте width:100%, object-fit:cover и понятный alt. Сравните резкость и размер файла до и после обработки.'],check:'Изображение не искажено, путь локальный, исходник сохранён.'},
{why:'Адаптация меняет размещение, а не уменьшает всю страницу до нечитаемого размера. При 390 px колонки превращаются в одну.',actions:['Откройте инструменты браузера F12 → режим устройства → 390 × 844.','В CSS добавьте перенос сетки и min-width:0 для её элементов. Проверьте каждую форму с ошибками, длинным ФИО и клавиатурой.'],code:'.cards { display: grid; grid-template-columns: repeat(auto-fit, minmax(min(100%, 280px), 1fr)); gap: 16px; }\ninput, select, textarea { width: 100%; box-sizing: border-box; }\n@media (max-width: 600px) { .form-row { display: block; } }',check:'Нет горизонтальной прокрутки; подписи и сообщения ошибок полностью видны.'},
{why:'Список ограничивает выбор известными значениями; сервер всё равно проверяет присланный идентификатор. Фильтр — это запрос к данным, а не скрытие случайных карточек.',actions:['Заполните select помещений записями rooms: value=id, видимый текст=name.','В админке отправляйте параметры фильтра через GET, проверяйте допустимые значения и применяйте условия WHERE. Покажите сообщение, если записей нет.'],check:'Очистка фильтра возвращает все доступные записи; неизвестное значение не ломает запрос.'},
{why:'Доступность проверяют действием: красивый скриншот не показывает, можно ли пройти форму клавиатурой.',actions:['Пройдите все поля клавишей Tab и отправьте форму без мыши. Не удаляйте outline без замены.','Поставьте масштаб 200%, вызовите ошибки, проверьте контраст. Сохраните desktop/mobile скриншоты в docs и зафиксируйте конец задания 2.'],code:'git status\ngit add static docs\ngit diff --cached --stat\ngit commit -m "Задание 2: стиль и адаптация проверены"',check:'Есть снимки всех обязательных страниц и коммит результата.'}
],
interaction:[
{why:'Интерфейс показывает состояние базы. После POST сервер сохраняет данные и перенаправляет на GET: обновление страницы не повторяет вставку.',actions:['Зафиксируйте начало задания 3: git commit --allow-empty -m "Начало задания 3". Затем откройте обработчик создания заявки: сначала проверка формы, затем INSERT с параметрами, затем redirect на кабинет.','Создайте одну заявку и обновите кабинет дважды. Сравните число записей в applications через phpMyAdmin.'],check:'Одна отправка создаёт одну запись; карточка показывает сохранённые дату, помещение и статус.'},
{why:'Обратная связь объясняет, что произошло. CSS отвечает за переходы, JavaScript — за локальные действия, сервер — за достоверность данных.',actions:['В JS выбранного стека найдите обработчик submit: он меняет подпись кнопки на «Отправка…» и блокирует повторное нажатие. Подключайте скрипт с defer.','Добавьте видимый :focus-visible и короткий transition в CSS. Для prefers-reduced-motion отключите анимации. В Flask дополнительно проверьте маску телефона, показ пароля и подсказку вместимости.','Отправьте неверные данные: после ответа страница снова должна позволять исправление и отправку. Не заменяйте серверную проверку JavaScript-проверкой.'],code:'button { transition: background-color .15s, transform .15s; }\nbutton:active { transform: translateY(1px); }\nbutton:focus-visible { outline: 3px solid #0d47a1; outline-offset: 3px; }\n@media (prefers-reduced-motion: reduce) { * { transition: none !important; animation: none !important; } }',check:'Видны успех и ошибки, двойной клик не создаёт две заявки, отключение JS не снимает серверные ограничения.'},
{why:'Скрытая кнопка не защищает данные: запрос можно отправить вручную. Поэтому правило отзыва проверяется в обработчике до INSERT.',actions:['Получите заявку по id и user_id из текущей сессии. Убедитесь, что её status — completed.','Только после этих проверок сохраните отзыв параметризованным запросом. UNIQUE(application_id) остановит повторную запись.','Проверьте три отказа: чужая заявка, незавершённая заявка, повторный отзыв. Затем проверьте успешный отзыв.'],check:'В reviews ровно одна запись для своей завершённой заявки, остальные попытки отклонены.'},
{why:'Индекс помогает найти строки, но занимает место и требует обновления. Выбираем его по реальному WHERE, а не для каждого поля.',actions:['В phpMyAdmin → SQL выполните EXPLAIN для запроса кабинета. Вместо 1 используйте id тестового пользователя.','Посмотрите possible_keys, key и rows. На маленьком каталоге MySQL может выбрать полный просмотр — это нормально. Сопоставьте индекс user_id со схемой.'],code:'EXPLAIN SELECT * FROM applications WHERE user_id = 1;\nSHOW INDEX FROM applications;',check:'Можете объяснить, какой запрос обслуживает каждый добавленный индекс.'},
{why:'Без интернета локальный сервер продолжает работать, если все ресурсы сохранены рядом с проектом. Зависимости устанавливаются заранее.',actions:['В Network найдите внешние домены. Замените CDN локальными CSS, JS, шрифтами и картинками с соблюдением лицензий.','Отключите интернет, сохранив работающими Apache/Flask и MySQL. Обновите страницы и пройдите вход → заявка. Не выключайте локальный сервер.'],check:'Нет внешних запросов, пропавших иконок и шрифтов; пользовательский сценарий работает.'},
{why:'Финальный коммит задания 3 должен содержать проверенную связку интерфейса и сервера, а не только анимацию.',actions:['В Console устраните ошибки JavaScript. В Network проверьте отсутствующие ресурсы и неожиданные ошибки HTTP.','Повторите весь сценарий двумя пользователями и администратором. Запишите результат и ограничения в docs/QA.md.'],code:'git status\ngit add .\ngit diff --cached --stat\ngit commit -m "Задание 3: интерактивность и целостность проверены"',check:'Перед git add . настроен .gitignore; в подготовленных изменениях нет секретов и личных данных.'}
],
delivery:[
{why:'Проверка в новой папке выявляет зависимости от случайных файлов и старой базы на вашем компьютере.',actions:['Скопируйте только сдаваемые файлы в отдельную папку. Создайте отдельную пустую учебную БД, не очищайте рабочую.','Выполните собственный README с нуля: зависимости, настройка подключения, создание таблиц и запуск. Для PHP используйте XAMPP, для Flask — виртуальное окружение и MySQL.'],check:'Другой человек может запустить проект только по README, без устных подсказок.'},
{why:'Тестируем не только успешный путь. Ошибка должна быть понятной, а запрещённое действие не должно менять базу.',actions:['Создайте двух тестовых пользователей. Проверьте пустые поля, занятый логин, неверный пароль и прямой адрес админки.','Создайте заявку первым пользователем, попробуйте открыть/изменить её вторым. Завершите мероприятие администратором и оставьте отзыв владельцем.','Запишите: действие → ожидаемый результат → фактический результат. Для готового Flask выполните python -m pytest -q; свои изменённые требования отразите в тестах.'],check:'Все отказы оставляют данные неизменными, успешный сценарий проходит полностью.'},
{why:'Коммит сохраняет работу локально, push передаёт её в удалённый репозиторий. Сначала проверьте адрес: сдача идёт в ваш индивидуальный репозиторий.',actions:['Создайте свой пустой репозиторий на GitHub. Скопируйте его HTTPS-адрес.','Проверьте подготовленные изменения и сделайте финальный коммит. В команде remote замените ВАШ_АДРЕС на свой адрес. Если origin уже есть, сначала проверьте git remote -v; не добавляйте его повторно.','После push откройте GitHub и проверьте файлы и историю. Начало и конец каждого задания должны быть видны в коммитах.'],code:'git status\ngit add .\ngit diff --cached --stat\ngit commit -m "Готово к сдаче: код, схема и инструкция"\ngit branch -M main\ngit remote -v\ngit remote add origin ВАШ_АДРЕС\ngit push -u origin main\ngit log --oneline',check:'В браузере видны свежий коммит и нужные файлы в вашем репозитории.'},
{why:'.gitignore предотвращает добавление новых файлов, но не удаляет уже отслеживаемые. Проверять нужно состав индекса и историю.',actions:['Выполните git ls-files: исключите окружение, кэш, .env и рабочие базы. Для уже отслеживаемого файла используйте git rm --cached с его точным путём, сохранив локальную копию.','Если секрет уже опубликован, замените его значение: удаление строки в новом коммите не отменяет старую утечку. Для учебных SQL оставьте только демонстрационные данные.'],code:'git ls-files\ngit status\ngit diff --cached',check:'В репозитории нет личных данных, паролей подключения и служебного окружения.'},
{why:'Результат — воспроизводимый проект и доказательства проверки. PDF-схема помогает прочитать модель, SQL позволяет её создать: нужны разные файлы.',actions:['Проверьте наличие исходников, локальных ресурсов, SQL, ER в PDF/изображении, README и источников ресурсов.','В README укажите стек, зависимости, имя учебной БД, команды запуска, тестовые учётные записи и сценарий проверки.','Сверьте работу с критериями базового уровня. Откройте сохранённую ER-схему и клонируйте/скачайте собственный репозиторий для последней проверки.'],check:'Ссылка открывается, все материалы читаются, инструкция приводит к работающему приложению.'}
]
};
