import http from 'node:http';import fs from 'node:fs';import path from 'node:path';
const root=path.resolve('site');const mime={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json','.png':'image/png','.webp':'image/webp','.pdf':'application/pdf','.zip':'application/zip'};
http.createServer((req,res)=>{try{const url=new URL(req.url,'http://localhost');const f=path.resolve(root,'.'+decodeURIComponent(url.pathname==='/'?'/index.html':url.pathname));if(!f.startsWith(root+path.sep)){res.writeHead(403).end();return;}res.setHeader('Content-Type',mime[path.extname(f)]||'text/plain; charset=utf-8');fs.createReadStream(f).on('error',()=>res.writeHead(404).end('Not found')).pipe(res);}catch{res.writeHead(400).end();}}).listen(Number(process.env.PORT||4197),'127.0.0.1',()=>console.log('http://127.0.0.1:'+(process.env.PORT||4197)));
